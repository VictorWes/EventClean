version: '3.8'
services:
  postgres:
    image: postgres:latest
    container_name: eventclean-db
    environment:
      POSTGRES_DB: eventclean
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"